CREATE TABLE student (
  id SERIAL PRIMARY KEY,
  name TEXT,
  email TEXT,
  age INTEGER,
  course TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO students (name, email, age, course)
VALUES
('Aarav Sharma', 'aarav@gmail.com', 21, 'Computer Science'),
('Diya Patel', 'diya@gmail.com', 19, 'Information Technology'),
('Rohan Mehta', 'rohan@gmail.com', 22, 'Computer Science'),
('Sneha Iyer', 'sneha@gmail.com', 20, 'Data Science'),
('Kunal Verma', 'kunal@gmail.com', 18, 'Information Technology');
SELECT * FROM students;
SELECT * FROM students
WHERE age > 20;
SELECT name, email FROM students;
SELECT * FROM students
WHERE course = 'Computer Science';
UPDATE students
SET age = 23
WHERE id = 1;
UPDATE students
SET course = 'AI & ML'
WHERE course = 'Data Science';
DELETE FROM students
WHERE id = 5;
DELETE FROM students
WHERE age < 20;
